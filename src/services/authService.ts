import { IUser } from '../interfaces';

const handleLogin = (data: IUser) => {};

const checkEmailExists = async (email: string) => {
  try {
    return false;
  } catch (error) {}
};

export default {};
